<script lang="ts">import { enhance } from '$app/forms'; import type { ActionData, PageServerData } from './$types'; let { data, form }: { data: PageServerData; form: ActionData } = $props();</script>
<svelte:head><title>Profile settings · LinkGarden</title><meta name="robots" content="noindex" /></svelte:head>
<section><h1>Profile settings</h1><p>Your permanent profile: <a href={'/@' + data.profile.username}>/@{data.profile.username}</a></p><form method="post" use:enhance><label>Display name <input required maxlength="80" name="displayName" value={data.profile.displayName} /></label><label>Bio <textarea maxlength="500" name="bio">{data.profile.bio ?? ''}</textarea></label><label>Avatar URL <input type="url" name="avatarUrl" value={data.profile.avatarUrl ?? ''} /></label>{#if form?.saved}<p class="success">Profile saved.</p>{/if}{#if form?.message}<p class="error">{form.message}</p>{/if}<button>Save profile</button></form></section>
