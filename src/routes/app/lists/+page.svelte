<script lang="ts">import { enhance } from '$app/forms'; import type { ActionData, PageServerData } from './$types'; let { data, form }: { data: PageServerData; form: ActionData } = $props();</script>
<svelte:head><title>My lists · LinkGarden</title><meta name="robots" content="noindex" /></svelte:head>
<section><h1>My lists</h1><form class="card" method="post" action="?/create" use:enhance><label>Title <input required maxlength="120" name="title" /></label><label>Permanent slug <input required maxlength="80" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" name="slug" placeholder="independent-web" /></label><label>Description <textarea maxlength="2000" name="description"></textarea></label><label>Visibility <select name="visibility"><option value="private">Private</option><option value="public">Public</option></select></label>{#if form?.message}<p class="error">{form.message}</p>{/if}<button>Create list</button></form></section>
<section class="stack"><h2>Your collections</h2>{#each data.lists as item}<article class="card"><h3><a href={'/app/lists/' + item.id}>{item.title}</a></h3><p>{item.description || 'No description yet.'}</p><p>{item.visibility} · {item.isOwner ? 'Owner' : 'Editor'}</p></article>{:else}<p>No lists yet.</p>{/each}</section>
