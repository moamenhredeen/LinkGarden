import { and, asc, eq, inArray, sql } from 'drizzle-orm'; import { error } from '@sveltejs/kit'; import { getDb } from '$lib/server/db'; import { link, linkTag, list, listMember, profile, tag } from '$lib/server/db/schema'; import type { PageServerLoad } from './$types';
export const load: PageServerLoad = async (event) => { const db = getDb(event.platform!.env.DB); const rows = await db.select({ collection: list, routeProfile: profile }).from(list).innerJoin(profile, eq(profile.userId, list.routeProfileId)).where(and(sql`lower(${profile.username}) = ${event.params.username.toLocaleLowerCase('en-US')}`, sql`lower(${list.slug}) = ${event.params.slug.toLocaleLowerCase('en-US')}`, eq(list.visibility, 'public'), eq(list.moderationState, 'active'))).limit(1); if (!rows[0]) error(404, 'List not found'); const collection = rows[0].collection; const links = await db.select().from(link).where(and(eq(link.listId, collection.id), eq(link.moderationState, 'active'))).orderBy(asc(link.position)); const tags = links.length ? await db.select({ linkId: linkTag.linkId, name: tag.name }).from(linkTag).innerJoin(tag, eq(tag.id, linkTag.tagId)).where(inArray(linkTag.linkId, links.map((item) => item.id))) : []; const owner = await db.select().from(profile).where(eq(profile.userId, collection.ownerUserId)).limit(1); const editors = await db.select({ username: profile.username, displayName: profile.displayName }).from(listMember).innerJoin(profile, eq(profile.userId, listMember.userId)).where(eq(listMember.listId, collection.id)); return { collection, routeProfile: rows[0].routeProfile, owner: owner[0], editors, links: links.map((item) => ({ ...item, tags: tags.filter((entry) => entry.linkId === item.id).map((entry) => entry.name) })) }; };
