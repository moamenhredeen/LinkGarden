import { and, desc, eq, isNotNull, sql } from 'drizzle-orm'; import { error } from '@sveltejs/kit'; import { getDb } from '$lib/server/db'; import { link, list, profile } from '$lib/server/db/schema'; import type { PageServerLoad } from './$types';
export const load: PageServerLoad = async (event) => { const db = getDb(event.platform!.env.DB); const profiles = await db.select().from(profile).where(sql`lower(${profile.username}) = ${event.params.username.toLocaleLowerCase('en-US')}`).limit(1); if (!profiles[0]) error(404, 'Profile not found'); const person = profiles[0]; const links = await db.select().from(link).where(and(eq(link.ownerUserId, person.userId), eq(link.visibility, 'public'), eq(link.moderationState, 'active'), isNotNull(link.ownerUserId))).orderBy(desc(link.publishedAt)); const lists = await db.select({ item: list, routeUsername: profile.username }).from(list).innerJoin(profile, eq(profile.userId, list.routeProfileId)).where(and(eq(list.ownerUserId, person.userId), eq(list.visibility, 'public'), eq(list.moderationState, 'active'))).orderBy(desc(list.updatedAt)); return { profile: person, links, lists }; };
