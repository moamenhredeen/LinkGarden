<script lang="ts">import type { PageServerData } from './$types'; let { data }: { data: PageServerData } = $props();</script>
<svelte:head><title>{data.q ? `Search: ${data.q}` : 'Search'} · LinkGarden</title></svelte:head><h1>Search the useful web</h1><form method="get"><div class="search-row"><label class="sr-only" for="q">Search</label><input id="q" name="q" value={data.q} placeholder="Try “independent publishing”" /><button>Search</button></div></form>
{#if data.q}<section class="stack"><h2>Results for “{data.q}”</h2>{#each data.results as item}<article class="card"><p class="eyebrow">{item.kind === 'list' ? 'List' : item.kind === 'list_link' ? 'Link in a list' : 'Personal link'}</p><h3>{#if item.kind === 'list'}<a href={'/@' + item.route_username + '/' + item.slug}>{item.title}</a>{:else}<a href={item.url} rel="noreferrer">{item.title}</a>{/if}</h3><p>{item.description}</p><p>{item.curators}</p>{#if item.kind === 'list_link'}<a href={'/@' + item.route_username + '/' + item.slug}>See it in context →</a>{/if}</article>{:else}<p>No public links or lists matched.</p>{/each}</section>{/if}
