<script lang="ts">import { enhance } from '$app/forms'; import type { ActionData, PageServerData } from './$types'; let { data, form }: { data: PageServerData; form: ActionData } = $props();</script>
<svelte:head><title>Choose your profile · LinkGarden</title><meta name="robots" content="noindex" /></svelte:head>
<section class="auth-card"><h1>Choose your public identity</h1><p>Your username becomes your permanent profile address.</p><form method="post" use:enhance><label>Username <input required minlength="3" maxlength="30" name="username" autocomplete="username" /></label><label>Display name <input required maxlength="80" name="displayName" value={data.displayName} /></label>{#if form?.message}<p class="error">{form.message}</p>{/if}<button>Create profile</button></form></section>
